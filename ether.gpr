with "aws.gpr";
with "templates_parser.gpr";
with "xmlada.gpr";

library project Ether is
   for Source_Dirs      use ("src");
   for Library_Name     use "ether";
   for Library_Kind     use "static";
   for Library_Dir      use "lib";
   for Externally_Built use "true";
end ether;
