with "ether.gpr";

Project Apps is
   for Source_Dirs use ("apps");
   for Object_Dir  use "obj";
   for Exec_Dir    use ".";
   for Main        use ("test.adb");

   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;
end Apps;
